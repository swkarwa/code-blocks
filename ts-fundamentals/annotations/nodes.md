# TS

## Compiling TS to JS

```code
    tsc <path-to-file>
```
creates a JS

## Type inference
  - it automatically detects variable type and throws error, no need to mention types

## any type
    - it is an escape hatch, it tuens off tpescript checking fot the variable

